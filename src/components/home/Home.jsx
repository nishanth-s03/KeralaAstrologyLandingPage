import { Card, CardContent, Container, Typography } from '@mui/material'

import './Home.css'

const Home = () => {
  return (
    <Container
      disableGutters
      maxWidth='xl'
      sx={{
        display: 'flex',
        flex: 1,
      }}
    ></Container>
  )
}

export default Home
